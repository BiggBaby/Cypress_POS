{
  "name": "cypresspos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:env": "echo \"Error: no test specified\" && exit 1",
    "test": "cypress open",
    "cypress:headless": "cypress run --headless --browser chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.4.0"
  }
}
